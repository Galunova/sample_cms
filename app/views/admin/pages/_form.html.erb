<%= form_for [:admin, @page], url: form_url, method: form_method do |f| %> 
    <% if @page.errors.any? %>
        <div class="errors">
          Total errors: <%= @page.errors.count %>
          <ul>
            <% @page.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :slug %><br/>
      <%= f.text_field :slug %>
    </p>

    <p>
      <%= f.label :text %><br/>
      <%= f.text_area :content %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>
